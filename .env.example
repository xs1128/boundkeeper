# Required for live analysis; fixtures and crisis routing do not use credentials.
OPENAI_API_KEY=
# Optional override; the default is a pinned structured-output model.
OPENAI_MODEL=gpt-4.1-mini-2025-04-14

# Optional stretch integrations.
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=
LINE_LIFF_ID=
